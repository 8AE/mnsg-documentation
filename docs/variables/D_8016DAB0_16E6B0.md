---
title: "Single-allocation telemetry counter"
sidebar_label: "D_8016DAB0_16E6B0"
slug: "/variables/D_8016DAB0_16E6B0"
description: "Unsigned 16-bit telemetry counter for single kind-2 allocation requests."
---

`D_8016DAB0_16E6B0` · **Variable**

Unsigned 16-bit telemetry counter for single kind-2 allocation requests.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x8016DAB0` | — | `ABSOLUTE_SYMS` |

## Signature

```c
extern unsigned short D_8016DAB0_16E6B0;
```

## How it works

Incremented before capacity checks and failures in func_80036448. It is therefore distinct from the active record count D_8016DADC. Its broader reset interval is not established here.

## Known values

### Counter representation {#counter-representation}

Single kind-2 allocation-request telemetry. It increments before capacity checks, so requests can be counted even when allocation fails.

| Storage type | Stored range | Interpretation |
| --- | --- | --- |
| `uint16_t` | `0x0000`–`0xFFFF` (`0`–`65535`) | Numeric counter, not an enum or bitmask. The broader reset interval is not established. |

## Usage example

```c
extern unsigned short D_8016DAB0_16E6B0;

unsigned short display_allocation_telemetry(void) {
    return D_8016DAB0_16E6B0;
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Treat engine counters as read-only outside a verified allocator patch; release paths depend on consistency.

## Related symbols

- [`D_8016DAB2_16E6B2`](../variables/D_8016DAB2_16E6B2.md)
- [`D_8016DADC_16E6DC`](../variables/D_8016DADC_16E6DC.md)
