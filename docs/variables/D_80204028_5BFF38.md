---
title: "Raw action-model file IDs"
sidebar_label: "D_80204028_5BFF38"
slug: "/variables/D_80204028_5BFF38"
description: "Four file IDs for raw playable-character action-model data."
---

`D_80204028_5BFF38` · **Variable**

Four file IDs for raw playable-character action-model data.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80204028` | `0x005BFF38` | `.file_11` |

## Signature

```c
extern unsigned short D_80204028_5BFF38[];
```

## How it works

Index order is Goemon, Ebisumaru, Sasuke, Yae. The action metadata in D_80203F34_5BFE44 supplies ranges within the corresponding raw file.

The whole file may be cached and shared read-only. Per-instance appearance substitutions require private writable slices rather than modification of the shared cache.

## Usage example

```c
extern unsigned short D_80204028_5BFF38[];

extern unsigned int func_80001D68_2968(unsigned int file_id);

extern unsigned int func_80001D94_2994(unsigned int file_id);

/* Return the raw ROM bounds; this does not load or allocate the file. */
int character_action_rom_range(unsigned int character,
                               unsigned int *start, unsigned int *end) {
    if (character >= 4u || !start || !end) return 0;
    unsigned int id = D_80204028_5BFF38[character];
    *start = func_80001D68_2968(id);
    *end = func_80001D94_2994(id);
    return *end > *start;
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Only index 0..3. Action records must be range-checked against the cached file length.

## Related symbols

- [`D_80203F34_5BFE44`](../variables/D_80203F34_5BFE44.md)
- [`D_80204020_5BFF30`](../variables/D_80204020_5BFF30.md)
