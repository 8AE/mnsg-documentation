---
title: "Current resolved actor definition pointer"
sidebar_label: "D_8015CDE0"
slug: "/variables/D_8015CDE0"
description: "Scratch pointer to the current resolved room actor definition."
---

`D_8015CDE0` · **Variable**

Scratch pointer to the current resolved room actor definition.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x8015CDE0` | — | `ABSOLUTE_SYMS` |

## Signature

```c
extern ActorDefinition *D_8015CDE0;
```

## How it works

The definition begins with unsigned short actor ID and parameters, followed by three 32-bit data words.

The native actor manager resolves definitions through the active actor-data file. func_80218A54_5D3F24 also replaces this pointer from its supplied instance before copying definition data into the task.

The partial definition layout begins with 16-bit actor ID and parameters, followed by three 32-bit payload words. Native initialization resolves the instance definition and updates this shared pointer.

Do not retain it as stable per-actor state across nested spawns or stage teardown.

## Usage example

```c
typedef struct { unsigned short actor_id, params; unsigned int data[3]; } ActorDefinition;
extern ActorDefinition *D_8015CDE0;

int current_spawn_actor_id(void)
{
    return D_8015CDE0 ? (int)D_8015CDE0->actor_id : -1;
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Notes

- Retain values rather than assuming this global still names the same definition after another spawn call.
- This is native scene/overlay state. Validate actor IDs and resource lifetime; do not assume an unsized extern provides bounds.

## Related symbols

- [`func_80218A54_5D3F24`](../functions/func_80218A54_5D3F24.md)
- [`func_8003555C_3615C`](../functions/func_8003555C_3615C.md)
- [`D_8015CDDC`](../variables/D_8015CDDC.md)
