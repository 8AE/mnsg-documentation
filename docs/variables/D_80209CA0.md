---
title: "Minimap native texture bank"
sidebar_label: "D_80209CA0"
slug: "/variables/D_80209CA0"
description: "Six 16-byte texture descriptors supply the room minimap, compass and local marker."
---

`D_80209CA0` · **Variable**

Six 16-byte texture descriptors supply the room minimap, compass and local marker.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80209CA0` | `0x005C5BB0` | `.file_0C` |

## Signature

```c
extern unsigned char D_80209CA0[6][16];
```

## How it works

Each entry stores u16 width at +0, u16 height at +2, format byte at +4, texture address at +8 and file ID at +0x0C.

Entry three is a 64 by 32 RGBA5551 sheet: format 0x10, segmented address 0x0A000000 and file 0x007F. It supplies the round position dot and some directional markers.

Entries four and five use the same dimensions and file, at 0x0A00B000 and 0x0A00D000. Their resources are 0x800F and 0x8011. Entries one and two are modified when a room map is initialized.

File zero means a direct texture pointer. A nonzero file requires the file's loaded base and segment mapping; do not interpret a segmented address as a CPU pointer.

An independently cropped texture needs matching bank dimensions, sprite UV origin, and S/T tile masks. The native sprite word at +0x08 stores S mask in bits 15..18 and T mask in bits 11..14; an 8 by 8 texture uses mask three for each axis.

## Native textures

The native sprite descriptors documented here select these images. The pause-overlay texture handle identifies another native interface to the same source sheet. Browse the [texture gallery](/textures/) for original sheets, ROM resource addresses, and matched recomp texture variables.

| Image | Meaning | ROM resource / address | Texture handle storage |
| --- | --- | --- | --- |
| ![Minimap position dot](/img/textures/minimap-dot.png) | Minimap position dot | [0x8004](/textures/?q=0x8004) · `0x007E6C00` | [`D_80217F6C_676F1C`](/variables/D_80217F6C_676F1C/) · `0x80217F6C` |

## Usage example

```c
extern unsigned char D_80209CA0[6][16];

unsigned int minimap_marker_file(void)
{
    return *(unsigned short *)(D_80209CA0[3] + 12);
}
```

The example illustrates the native interface and its required state.

## Notes

- Only access this layout while its owning overlay and task are active. Task-pool addresses can be reused after destruction.
- The table stores texture descriptors, not the opaque texture handles used by the pause-menu image system.
- A direct pixel pointer must be readable by the graphics processor. In the stock graphics path, keep image bytes in original RDRAM below 0x80800000. Extended CPU addresses can be interpreted as segmented addresses and sample unrelated memory; a valid CPU pointer alone is insufficient.
- Texture bank and sprite descriptors are read by the CPU, but their referenced pixels survive until queued graphics work finishes. Scene-arena pixel buffers must be reserved before later allocations and invalidated when the native scene registry resets.

## Related symbols

- [`D_80167FC0_168BC0`](../variables/D_80167FC0_168BC0.md)
