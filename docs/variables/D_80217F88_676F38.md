---
title: "Silver Ebisumaru texture handle"
sidebar_label: "D_80217F88_676F38"
slug: "/variables/D_80217F88_676F38"
description: "Runtime texture handle for native image resource 0x800B, initialized by the pause-menu overlay."
---

`D_80217F88_676F38` · **Variable**

Runtime texture handle for native image resource 0x800B, initialized by the pause-menu overlay.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80217F88` | — | `.file_17 runtime storage` |

## Signature

```c
extern unsigned int D_80217F88_676F38;
```

## How it works

The pause-menu initializer func_80211870_670820 stores the return from the native texture registration routine at 0x80217F88. The matching recomp data symbol is D_80217F88_676F38.

The registered image is resource 0x800B, stored at ROM address 0x007E8A80. Its decoded RGBA5551 sheet is 64 × 32 pixels.

This word holds an opaque runtime texture handle, not pixel bytes or a ROM pointer. The named storage is beyond the file-backed pause overlay; the numeric suffix in its symbol name does not identify the packed image resource.

## Native textures

This variable holds the native texture handle for the sheet below. Browse the [texture gallery](/textures/) for original sheets, ROM resource addresses, and matched recomp texture variables.

![Resource 0x800B sheet](/img/textures/resource-800b.png)

[0x800B in the gallery](/textures/?q=D_80217F88_676F38) · 64 × 32 pixels · packed ROM address `0x007E8A80`.

| Image | Meaning | ROM resource / address | Texture handle storage |
| --- | --- | --- | --- |
| ![Silver Ebisumaru](/img/textures/silver_ebisumaru.png) | Silver Ebisumaru | [0x800B](/textures/?q=0x800B) · `0x007E8A80` | [`D_80217F88_676F38`](/variables/D_80217F88_676F38/) · `0x80217F88` |

## Usage example

```c
extern unsigned int D_80217F88_676F38;

/* Read only after the pause overlay has registered its textures. */
unsigned int read_pause_texture_handle(void)
{
    return D_80217F88_676F38;
}
```

Observe this value while the pause-menu overlay is active and its texture initialization has completed. Pass handles only to native interfaces that expect them.

## Notes

- The runtime address is reused by other overlays. Do not read or write this variable when the pause overlay is inactive.
- Texture handles are assigned at runtime; no fixed handle value or enum meaning is established.

## Related symbols

- [`func_800144E8_150E8`](../functions/func_800144E8_150E8.md)
