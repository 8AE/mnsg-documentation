---
title: "Bazooka, Gold Yae texture handle"
sidebar_label: "D_80217F7C_676F2C"
slug: "/variables/D_80217F7C_676F2C"
description: "Runtime texture handle for native image resource 0x8008, initialized by the pause-menu overlay."
---

`D_80217F7C_676F2C` · **Variable**

Runtime texture handle for native image resource 0x8008, initialized by the pause-menu overlay.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80217F7C` | — | `.file_17 runtime storage` |

## Signature

```c
extern unsigned int D_80217F7C_676F2C;
```

## How it works

The pause-menu initializer func_80211870_670820 stores the return from the native texture registration routine at 0x80217F7C. The matching recomp data symbol is D_80217F7C_676F2C.

The registered image is resource 0x8008, stored at ROM address 0x007E7EA0. Its decoded RGBA5551 sheet is 64 × 32 pixels.

This word holds an opaque runtime texture handle, not pixel bytes or a ROM pointer. The named storage is beyond the file-backed pause overlay; the numeric suffix in its symbol name does not identify the packed image resource.

## Native textures

This variable holds the native texture handle for the sheet below. Browse the [texture gallery](/textures/) for original sheets, ROM resource addresses, and matched recomp texture variables.

![Resource 0x8008 sheet](/img/textures/resource-8008.png)

[0x8008 in the gallery](/textures/?q=D_80217F7C_676F2C) · 64 × 32 pixels · packed ROM address `0x007E7EA0`.

| Image | Meaning | ROM resource / address | Texture handle storage |
| --- | --- | --- | --- |
| ![Bazooka](/img/textures/bazooka.png) | Bazooka | [0x8008](/textures/?q=0x8008) · `0x007E7EA0` | [`D_80217F7C_676F2C`](/variables/D_80217F7C_676F2C/) · `0x80217F7C` |
| ![Gold Yae](/img/textures/gold_yae.png) | Gold Yae | [0x8008](/textures/?q=0x8008) · `0x007E7EA0` | [`D_80217F7C_676F2C`](/variables/D_80217F7C_676F2C/) · `0x80217F7C` |

## Usage example

```c
extern unsigned int D_80217F7C_676F2C;

/* Read only after the pause overlay has registered its textures. */
unsigned int read_pause_texture_handle(void)
{
    return D_80217F7C_676F2C;
}
```

Observe this value while the pause-menu overlay is active and its texture initialization has completed. Pass handles only to native interfaces that expect them.

## Notes

- The runtime address is reused by other overlays. Do not read or write this variable when the pause overlay is inactive.
- Texture handles are assigned at runtime; no fixed handle value or enum meaning is established.

## Related symbols

- [`func_800144E8_150E8`](../functions/func_800144E8_150E8.md)
