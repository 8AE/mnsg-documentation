---
title: "Achilles Heel, Cucumber texture handle"
sidebar_label: "D_80217F1C_676ECC"
slug: "/variables/D_80217F1C_676ECC"
description: "Runtime texture handle for native image resource 0x8693, initialized by the pause-menu overlay."
---

`D_80217F1C_676ECC` · **Variable**

Runtime texture handle for native image resource 0x8693, initialized by the pause-menu overlay.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80217F1C` | — | `.file_17 runtime storage` |

## Signature

```c
extern unsigned int D_80217F1C_676ECC;
```

## How it works

The pause-menu initializer func_80211870_670820 stores the return from the native texture registration routine at 0x80217F1C. The matching recomp data symbol is D_80217F1C_676ECC.

The registered image is resource 0x8693, stored at ROM address 0x013F6080. Its decoded RGBA5551 sheet is 64 × 32 pixels.

This word holds an opaque runtime texture handle, not pixel bytes or a ROM pointer. The named storage is beyond the file-backed pause overlay; the numeric suffix in its symbol name does not identify the packed image resource.

## Native textures

This variable holds the native texture handle for the sheet below. Browse the [texture gallery](/textures/) for original sheets, ROM resource addresses, and matched recomp texture variables.

![Resource 0x8693 sheet](/img/textures/resource-8693.png)

[0x8693 in the gallery](/textures/?q=D_80217F1C_676ECC) · 64 × 32 pixels · packed ROM address `0x013F6080`.

| Image | Meaning | ROM resource / address | Texture handle storage |
| --- | --- | --- | --- |
| ![Achilles Heel](/img/textures/achilles_heel.png) | Achilles Heel | [0x8693](/textures/?q=0x8693) · `0x013F6080` | [`D_80217F1C_676ECC`](/variables/D_80217F1C_676ECC/) · `0x80217F1C` |
| ![Cucumber](/img/textures/cucumber.png) | Cucumber | [0x8693](/textures/?q=0x8693) · `0x013F6080` | [`D_80217F1C_676ECC`](/variables/D_80217F1C_676ECC/) · `0x80217F1C` |

## Usage example

```c
extern unsigned int D_80217F1C_676ECC;

/* Read only after the pause overlay has registered its textures. */
unsigned int read_pause_texture_handle(void)
{
    return D_80217F1C_676ECC;
}
```

Observe this value while the pause-menu overlay is active and its texture initialization has completed. Pass handles only to native interfaces that expect them.

## Notes

- The runtime address is reused by other overlays. Do not read or write this variable when the pause overlay is inactive.
- Texture handles are assigned at runtime; no fixed handle value or enum meaning is established.

## Related symbols

- [`func_800144E8_150E8`](../functions/func_800144E8_150E8.md)
