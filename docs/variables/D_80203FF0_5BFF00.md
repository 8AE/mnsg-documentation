---
title: "Initial auxiliary segment descriptors"
sidebar_label: "D_80203FF0_5BFF00"
slug: "/variables/D_80203FF0_5BFF00"
description: "Initial face/part segment descriptors for two auxiliary channels."
---

`D_80203FF0_5BFF00` · **Variable**

Initial face/part segment descriptors for two auxiliary channels.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80203FF0` | `0x005BFF00` | `.file_11` |

## Signature

```c
extern unsigned char D_80203FF0_5BFF00[];
```

## How it works

Each channel uses two bytes. The second byte selects the object segment whose base is written at +0x38 + segment*8 when the action initial auxiliary resource is staged.

The first byte purpose is not established by the documented lookup.

## Usage example

```c
extern unsigned char D_80203FF0_5BFF00[];

/* The returned value still needs validation for the target object. */
int initial_aux_segment(unsigned int channel) {
    if (channel >= 2u) return -1;
    return D_80203FF0_5BFF00[channel * 2 + 1];
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- The first descriptor byte is not explained by the documented lookup; do not invent a meaning for it.

## Related symbols

- [`D_80203FF8_5BFF08`](../variables/D_80203FF8_5BFF08.md)
- [`func_800145B4_151B4`](../functions/func_800145B4_151B4.md)
