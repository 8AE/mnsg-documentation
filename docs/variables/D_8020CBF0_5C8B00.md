---
title: "Scene sound-state buffer"
sidebar_label: "D_8020CBF0_5C8B00"
slug: "/variables/D_8020CBF0_5C8B00"
description: "Native scene sound-state storage used by the object-position sound helper."
---

`D_8020CBF0_5C8B00` · **Variable**

Native scene sound-state storage used by the object-position sound helper.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x8020CBF0` | — | `ABSOLUTE_SYMS` |

## Signature

```c
extern unsigned char D_8020CBF0_5C8B00[];
```

## How it works

Pass the base address unchanged as the sound-state argument to func_8000F420_10020.

The internal structure and complete size have not been established. An unsigned-byte-array declaration preserves the known opaque pointer usage.

## Known values

### Opaque sound-state storage {#opaque-storage}

No individual field, byte value, or complete structure size has been established for this buffer. It does not have a known value enum.

| Representation | Interpretation |
| --- | --- |
| Address of byte-array storage | Pass the base address unchanged to the native scene sound helper; individual bytes are not documented controls. |

## Usage example

```c
extern unsigned char D_8020CBF0_5C8B00[];

void *native_scene_sound_state(void)
{
    return D_8020CBF0_5C8B00;
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.
