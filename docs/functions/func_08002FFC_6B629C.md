---
title: "Enter the Tsurami native death state"
sidebar_label: "func_08002FFC_6B629C"
slug: "/functions/func_08002FFC_6B629C"
description: "Enters Tsurami native death state through its custom hit callback."
---

`func_08002FFC_6B629C` · **Function**

Enters Tsurami native death state through its custom hit callback.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x08002FFC` | `0x006B629C` | `.file_29` |

Native code size: **92 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

Reached when the health-root callback accepts the lethal remaining-health condition. This is the onset of death handling, before destruction and teardown have completed.

Use func_08003A00 as the later terminal boundary when an operation depends on completed destruction.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_08002FFC_6B629C")
void observe_08002ffc(void *actor) {
    if (!actor) return;
    /* Match the tracked Tsurami root and room.
       Record the encounter defeat once. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Death entry is earlier than completed destruction; reward/progression should wait for the terminal callback.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_08000388_6B3628`](../functions/func_08000388_6B3628.md)
- [`func_08003A00_6B6CA0`](../functions/func_08003A00_6B6CA0.md)
