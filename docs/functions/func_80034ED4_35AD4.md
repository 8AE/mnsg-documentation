---
title: "Delete the current task"
sidebar_label: "func_80034ED4_35AD4"
slug: "/functions/func_80034ED4_35AD4"
description: "Deletes the scheduler current task."
---

`func_80034ED4_35AD4` · **Function**

Deletes the scheduler current task.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80034ED4` | `0x00035AD4` | `.main` |

Native code size: **36 bytes**.

## Signature

```c
extern void func_80034ED4_35AD4(void);
```

## How it works

Passes D_8016DAB4_16E6B4 to the native task-deletion helper FUN_80034EF8.

The common actor post invokes this routine when task status has the removal-pending flag 0x02. A custom task callback can also use it to end a finite lifetime.

## Usage example

```c
extern void func_80034ED4_35AD4(void);

void short_lived_effect_update(void *task, void *object)
{
    (void)object;
    unsigned short *timer = (unsigned short *)((char *)task + 0x8A);
    if (*timer == 0) {
        func_80034ED4_35AD4();
        return;
    }
    --*timer;
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Notes

- Return immediately after deletion and do not access the old task/object storage.
- This helper targets the current task rather than accepting an arbitrary task pointer.

## Native implementation

Ghidra pseudocode recovered at the displayed native address. Undefined types and analysis names are not complete C declarations; use the documented signature when writing calling code.

```c
void FUN_80034ed4(void)

{
  FUN_80034ef8(DAT_8016dab4);
  return;
}
```

## Related symbols

- [`D_8016DAB4_16E6B4`](../variables/D_8016DAB4_16E6B4.md)
- [`func_80218F30_5D4400`](../functions/func_80218F30_5D4400.md)
