---
title: "Dispatch the current system step"
sidebar_label: "func_80002040_2C40"
slug: "/functions/func_80002040_2C40"
description: "Dispatches the current system-state handler selected by the system step byte."
---

`func_80002040_2C40` · **Function**

Dispatches the current system-state handler selected by the system step byte.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x80002040` | `0x00002C40` | `.main` |

Native code size: **120 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

Copies the handler table at 0x80058908 into local storage, then calls the entry indexed by system->stepw through D_8015C5C8_15D1C8.

The selected handler depends on the current system phase, so this function is a useful boundary for observing state changes across menus, transitions and gameplay. A return hook observes state after that handler completes.

Indexes a native callback table with the byte g_system->stepw and invokes that entry. The selected work can belong to title screens, menus, gameplay or transitions.

A return hook runs after that selected step completes and is a useful recurring integration boundary. It does not guarantee a loaded save or a live gameplay actor.

## Return value

No return value.

## Usage example

```c
#include "modding.h"
extern unsigned char D_8015C608_15D208[];

static unsigned char observed_hp;
RECOMP_HOOK_RETURN("func_80002040_2C40")
void read_hp_after_system_step(void)
{
    char *save = (char *)D_8015C608_15D208;
    if (*(volatile int *)(save - 0x28) <= 0) return;
    observed_hp = *(volatile unsigned char *)(save - 0x21);
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Notes

- This dispatcher is not specific to gameplay. Check that player/save state is loaded before reading gameplay fields.
- Do not call this dispatcher recursively from its own hook.
- Return hooks have no original arguments or return-value parameter. Guard against title screens, transitions, and stale actor pointers.

## Native implementation

Ghidra pseudocode recovered at the displayed native address. Undefined types and analysis names are not complete C declarations; use the documented signature when writing calling code.

```c
void FUN_80002040(void)
{
  undefined4 *puVar1;
  undefined **ppuVar2;
  undefined **ppuVar3;
  undefined4 local_50[20];
  puVar1 = local_50;
  ppuVar2 = PTR_ARRAY_80058908;
  do {
    ppuVar3 = ppuVar2 + 3;
    *puVar1 = *ppuVar2;
    puVar1[1] = ppuVar2[1];
    puVar1[2] = ppuVar2[2];
    puVar1 = puVar1 + 3;
    ppuVar2 = ppuVar3;
  } while (ppuVar3 != (undefined **)&DAT_80058950);
  (*(code *)local_50[(byte)PTR_g_system_8015c5c8->stepw])();
  return;
}
```

## Related symbols

- [`D_8015C608_15D208`](../variables/D_8015C608_15D208.md)
- [`D_8015C5C8_15D1C8`](../variables/D_8015C5C8_15D1C8.md)
- [`func_80003728_4328`](../functions/func_80003728_4328.md)
