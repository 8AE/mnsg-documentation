---
title: "Consume a Dharmanyo life"
sidebar_label: "func_0800432C_6CC53C"
slug: "/functions/func_0800432C_6CC53C"
description: "Consumes one Dharmanyo boss life during native hit handling."
---

`func_0800432C_6CC53C` · **Function**

Consumes one Dharmanyo boss life during native hit handling.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x0800432C` | `0x006CC53C` | `.file_31` |

Native code size: **1256 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

At entry, hit timer +0xD6 equal to zero and lives +0xD1 equal to one identify the final-life condition. The original callback decrements to zero and starts the destruction chain.

Not every invocation is a final defeat. After last-life onset, a separate post-fight controller still owns gauge/fade and terminal cleanup.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_0800432C_6CC53C")
void observe_0800432c(void *actor) {
    if (!actor) return;
    unsigned short timer = *(unsigned short *)((char *)actor + 0xD6);
    unsigned char lives = ((unsigned char *)actor)[0xD1];
    if (timer == 0 && lives == 1) {
        /* Last-life callback is beginning. */
    }
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Do not equate every hit callback with final defeat; validate lives and hit timer.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_08003F84_6CC194`](../functions/func_08003F84_6CC194.md)
- [`func_0800636C_6CE57C`](../functions/func_0800636C_6CE57C.md)
