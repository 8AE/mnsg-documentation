---
title: "Congo health and damage-threshold controller"
sidebar_label: "func_0800A228_6BD4C8"
slug: "/functions/func_0800A228_6BD4C8"
description: "Processes Congo health thresholds and defeat state."
---

`func_0800A228_6BD4C8` · **Function**

Processes Congo health thresholds and defeat state.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x0800A228` | `0x006BD4C8` | `.file_29` |

Native code size: **952 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

The health-bearing root is entity 0x323. Relevant fields are health byte +0x8D and controller flag 0x04000000 at +0xE8.

Health thresholds 25, 20, 15, 10 and 5 generate transient event 0x0B for the spin reaction. At zero HP, native defeat handling restores HP to one before scheduling the victory sequence.

Congo zero-health handling schedules func_08007D24 as its victory callback. Tsurami roots can also pass through this helper, so identifying Congo requires its entity ID 0x323 at actor+0x5E.

This boundary can run during actor setup before the first main combat update. Capture only the health-bearing root and wait for initialized state before interpreting it.

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `task` | `void *` | Native task supplied at routine entry. Only this consumed argument is established here; additional native parameters have not been confirmed. |

## Return value

No return value is used by the documented task-callback convention.

## Usage example

```c
#include "modding.h"

static unsigned short last_entity;

RECOMP_HOOK("func_0800A228_6BD4C8")
void observe_native_state(void *task)
{
    if (!task) return;
    last_entity = *(volatile unsigned short *)((char *)task + 0x5E);
}
```

Observe entry into this native routine and sample the task entity field. The original function continues executing in its normal scheduler context.

## Notes

- A health value of one alone is not proof that normal combat remains active. Native defeat state must also be considered.
- Resolve the full ROM-qualified overlay symbol; a shared 0x080... address alone does not identify the loaded routine.
- Use a live task of the documented family. Task pointers and callback slots follow the 32-bit target ABI.
- A visual component is not authoritative boss health. Validate the actor ID and current encounter.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`D_8016DAB4_16E6B4`](../variables/D_8016DAB4_16E6B4.md)
- [`func_08007D24_6BAFC4`](../functions/func_08007D24_6BAFC4.md)
- [`func_80218E7C_5D434C`](../functions/func_80218E7C_5D434C.md)
