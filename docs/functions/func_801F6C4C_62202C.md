---
title: "Dispatch Thaisamba collision and damage"
sidebar_label: "func_801F6C4C_62202C"
slug: "/functions/func_801F6C4C_62202C"
description: "Dispatches Thaisamba collision and damage processing once per native battle frame."
---

`func_801F6C4C_62202C` · **Function**

Dispatches Thaisamba collision and damage processing once per native battle frame.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x801F6C4C` | `0x0062202C` | `.file_13` |

Native code size: **952 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

This collision pass is separate from the boss task dynamic AI callback, which handles movement and attack progression.

A return hook can observe completion of the native damage pass without executing another AI or collision update.

## Usage example

```c
#include "modding.h"

static unsigned int thaisamba_collision_frames;
RECOMP_HOOK_RETURN("func_801F6C4C_62202C")
void observe_thaisamba_collision_completion(void)
{
    ++thaisamba_collision_frames;
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Notes

- The complete native argument and return ABI is not established here. The return hook uses no original arguments.

## Related symbols

- [`func_801EF2E0_61A6C0`](../functions/func_801EF2E0_61A6C0.md)
- [`D_8020EED0_63A2B0`](../variables/D_8020EED0_63A2B0.md)
