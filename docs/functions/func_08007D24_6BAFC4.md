---
title: "Congo native victory callback"
sidebar_label: "func_08007D24_6BAFC4"
slug: "/functions/func_08007D24_6BAFC4"
description: "Congo native victory callback scheduled after its zero-health branch."
---

`func_08007D24_6BAFC4` · **Function**

Congo native victory callback scheduled after its zero-health branch.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x08007D24` | `0x006BAFC4` | `.file_29` |

Native code size: **200 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

Marks the transition into the official victory/reward sequence. It occurs later than generic damage reaching zero health.

Observe this boundary when an operation must wait for native victory handling. Calling it manually does not reproduce all prior combat and teardown steps.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_08007D24_6BAFC4")
void observe_08007d24(void *actor) {
    if (!actor) return;
    /* Match actor to the tracked Congo root.
       Native victory is now reaching its callback. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Do not call this callback manually to bypass Congo damage and teardown.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_0800A228_6BD4C8`](../functions/func_0800A228_6BD4C8.md)
