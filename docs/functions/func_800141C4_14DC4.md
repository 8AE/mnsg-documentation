---
title: "Look up a loaded resource file"
sidebar_label: "func_800141C4_14DC4"
slug: "/functions/func_800141C4_14DC4"
description: "Looks up a file ID in the native loaded-resource registry without loading missing data."
---

`func_800141C4_14DC4` · **Function**

Looks up a file ID in the native loaded-resource registry without loading missing data.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x800141C4` | `0x00014DC4` | `.main` |

Native code size: **84 bytes**.

## Signature

```c
extern void *func_800141C4_14DC4(unsigned int file_id);
```

## How it works

Scans registry entries beginning at 0x80167FC0 until a matching ID or the zero-ID terminator. A match returns the stored resource data pointer.

File ID zero returns null. A nonzero ID absent from the registry returns target pointer 0xFFFFFFFF.

Scans D_80167FC0_168BC0 until a matching file ID or the zero-file sentinel. ID zero returns null; an absent nonzero ID returns 0xFFFFFFFF.

Use this to obtain loaded data and to preflight segmented-pointer resolution. A successful lookup does not extend the resource lifetime past scene teardown.

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file_id` | `unsigned int` | Native resource file ID, such as 0x3B for the Ryo popup. |

## Return value

Loaded resource pointer; null for file ID 0; target pointer 0xFFFFFFFF when not found.

## Usage example

```c
extern void *func_800141C4_14DC4(unsigned int file_id);

void *find_ryo_popup_resource(void)
{
    void *resource = func_800141C4_14DC4(0x3B);
    if (!resource || resource == (void *)(unsigned long)0xFFFFFFFFu)
        return 0;
    return resource;
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Notes

- Check both null and the -1 sentinel before dereferencing. A non-null pointer alone does not prove the file was found.
- Registry entries have scene/overlay lifetimes; a pointer retained across a resource reload may be stale.
- Checking only null is insufficient: missing nonzero files return (void *)-1.
- Registered bases may carry the cache tag used by the engine. Only normalize addresses according to the destination subsystem.
- A successful lookup does not extend the resource lifetime beyond the current scene.

## Native implementation

Ghidra pseudocode recovered at the displayed native address. Undefined types and analysis names are not complete C declarations; use the documented signature when writing calling code.

```c
void * FUN_800141c4(uint param_1)

{
  uint uVar1;
  overlay *poVar2;

  poVar2 = overlay_ARRAY_80167fc0;
  if (param_1 == 0) {
    return (void *)0x0;
  }
  if (overlay_ARRAY_80167fc0[0].file_id != 0) {
    uVar1 = (uint)overlay_ARRAY_80167fc0[0].file_id;
    do {
      if (param_1 == uVar1) {
        return poVar2->data;
      }
      uVar1 = (uint)poVar2[1].file_id;
      poVar2 = poVar2 + 1;
    } while (uVar1 != 0);
  }
  return (void *)0xffffffff;
}
```

## Related symbols

- [`func_80013B14_14714`](../functions/func_80013B14_14714.md)
- [`func_802171A8_5D2678`](../functions/func_802171A8_5D2678.md)
- [`D_80167FC0_168BC0`](../variables/D_80167FC0_168BC0.md)
