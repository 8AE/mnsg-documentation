---
title: "Prepare destination scene loading"
sidebar_label: "func_8000B364_BF64"
slug: "/functions/func_8000B364_BF64"
description: "Prepares loading of the configured destination scene."
---

`func_8000B364_BF64` · **Function**

Prepares loading of the configured destination scene.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x8000B364` | `0x0000BF64` | `.main` |

Native code size: **128 bytes**.

## Signature

```c
extern void func_8000B364_BF64(void);
```

## How it works

Copies destination stage into active scene fields, calculates region/substage, begins scene-load bookkeeping and resets transition counters.

Supply a valid destination before invoking it. Follow the native transition sequence, including render reset and control/UI initialization; this helper alone is not a complete warp.

## Return value

No return value in the established declaration.

## Usage example

```c
extern void func_80003478_4078(void);

extern void func_8000383C_443C(void);

extern void func_8000B364_BF64(void);

/* Requires the native start/load preparation described above.
 * This helper is one step of that sequence, not a complete launch API. */
void prepare_destination_scene(void) {
    /* First prepare a valid native destination and save spawn fields. */
    func_8000B364_BF64();
    func_8000383C_443C();
    func_80003478_4078();
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Changing only current room or calling this with stale destination fields can load inconsistent state.

## Related symbols

- [`func_8000607C_6C7C`](../functions/func_8000607C_6C7C.md)
- [`func_8000383C_443C`](../functions/func_8000383C_443C.md)
