---
title: "Adjust the player Ryo balance"
sidebar_label: "func_801DCCF0_598C00"
slug: "/functions/func_801DCCF0_598C00"
description: "Applies a signed change to the player Ryo balance."
---

`func_801DCCF0_598C00` · **Function**

Applies a signed change to the player Ryo balance.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x801DCCF0` | `0x00598C00` | `.file_11` |

Native code size: **88 bytes**.

## Signature

```c
extern int func_801DCCF0_598C00(short delta);
```

## How it works

The documented delta is a signed short. Passing -50 requests a fifty-Ryo subtraction through the native balance routine.

Currency changes and visual/audio feedback are separate operations; this function alone does not establish a floating number or sound effect. The exact return meaning and complete clamp rules remain unconfirmed.

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `delta` | `short` | Signed Ryo change; observed -50 subtracts fifty Ryo through native logic. |

## Return value

The documented declaration returns int. Its exact meaning is not yet established.

## Usage example

```c
extern int func_801DCCF0_598C00(short delta);

/* Call only for a confirmed applied damage event in a loaded save. */
void apply_native_ryo_penalty(void)
{
    (void)func_801DCCF0_598C00(-50);
}
```

The example shows the documented native data layout or call sequence. Use it only while the relevant game state and resources are valid.

## Related symbols

- [`func_801DCD48_598C58`](../functions/func_801DCD48_598C58.md)
