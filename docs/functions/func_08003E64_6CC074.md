---
title: "Initialize the Dharmanyo boss controller"
sidebar_label: "func_08003E64_6CC074"
slug: "/functions/func_08003E64_6CC074"
description: "Initializes the actual Dharmanyo boss controller with twelve lives."
---

`func_08003E64_6CC074` · **Function**

Initializes the actual Dharmanyo boss controller with twelve lives.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x08003E64` | `0x006CC074` | `.file_31` |

Native code size: **276 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

The boss controller is entity 0xCC and its lives byte is +0xD1. Ordinary entity 0x132 is a separate bouncing Darumanyo enemy.

Treat initializer entry as fresh encounter evidence; wait for the following initialized combat update before reading authoritative lives.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_08003E64_6CC074")
void observe_08003e64(void *actor) {
    if (!actor) return;
    /* Capture the controller setup event.
       Wait for the first initialized combat update before reading lives. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Use entity 0xCC for this boss; ordinary enemy 0x132 has a different role.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_08003F84_6CC194`](../functions/func_08003F84_6CC194.md)
