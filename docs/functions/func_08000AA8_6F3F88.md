---
title: "Initialize a locked doorway"
sidebar_label: "func_08000AA8_6F3F88"
slug: "/functions/func_08000AA8_6F3F88"
description: "Initializes a native locked doorway and its optional padlock child."
---

`func_08000AA8_6F3F88` · **Function**

Initializes a native locked doorway and its optional padlock child.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x08000AA8` | `0x006F3F88` | `.file_43` |

Native code size: **1400 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

The initializer checks persistent lock state when constructing the door. A previously unlocked door can be initialized without creating a padlock child.

An entry hook can coordinate state that must be visible before construction. Do not start a live child-removal animation if no child was created to signal completion.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_08000AA8_6F3F88")
void inspect_door_initialization(void *actor, void *unused) {
    (void)unused;
    if (!actor) return;
    unsigned short lock_flag = *(unsigned short *)((char *)actor + 0xD4);
    /* Match a queued same-room unlock before native initialization. */
    (void)lock_flag;
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Do not force the live child-removal state if the padlock was never spawned: no child exists to signal completion.
- The hook signature shows the captured actor and ignored second argument; it is not a complete verified native return ABI.

## Related symbols

- [`func_08001020_6F4500`](../functions/func_08001020_6F4500.md)
- [`func_800240DC_24CDC`](../functions/func_800240DC_24CDC.md)
