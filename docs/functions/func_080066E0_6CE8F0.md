---
title: "Finish Dharmanyo post-fight teardown"
sidebar_label: "func_080066E0_6CE8F0"
slug: "/functions/func_080066E0_6CE8F0"
description: "Completes Dharmanyo post-fight teardown after the final wait."
---

`func_080066E0_6CE8F0` · **Function**

Completes Dharmanyo post-fight teardown after the final wait.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x080066E0` | `0x006CE8F0` | `.file_31` |

Native code size: **12 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

The terminal callback sets internal completion flag 0x16D and tears down the fight.

This is a stronger completion boundary than lives zero or a reward bit. Validate the post-fight controller instance rather than assuming it shares the combat actor address.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_080066E0_6CE8F0")
void observe_080066e0(void *actor) {
    if (!actor) return;
    /* Match the tracked post-fight controller and room.
       Record completion before native cleanup completes. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- Completion is tied to this terminal callback, not just lives zero or a reward bit.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_0800636C_6CE57C`](../functions/func_0800636C_6CE57C.md)
