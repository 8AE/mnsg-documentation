---
title: "Finish Tsurami destruction"
sidebar_label: "func_08003A00_6B6CA0"
slug: "/functions/func_08003A00_6B6CA0"
description: "Finishes Tsurami destruction and tears down its root task."
---

`func_08003A00_6B6CA0` · **Function**

Finishes Tsurami destruction and tears down its root task.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x08003A00` | `0x006B6CA0` | `.file_29` |

Native code size: **68 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

Scheduled after native destruction flag 0x1C1 and the post-destruction wait.

The actor can be removed by the original callback. Invalidate retained handles rather than dereferencing the root blindly afterward.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_08003A00_6B6CA0")
void observe_08003a00(void *actor) {
    if (!actor) return;
    /* Match the tracked root before native teardown.
       This is the completion boundary for dependent progression. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- The original callback destroys the root; retained handles must be invalidated.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_08002FFC_6B629C`](../functions/func_08002FFC_6B629C.md)
