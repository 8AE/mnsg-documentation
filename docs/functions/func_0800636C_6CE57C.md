---
title: "Update Dharmanyo post-fight controller"
sidebar_label: "func_0800636C_6CE57C"
slug: "/functions/func_0800636C_6CE57C"
description: "Updates the Dharmanyo post-fight gauge/fade controller."
---

`func_0800636C_6CE57C` · **Function**

Updates the Dharmanyo post-fight gauge/fade controller.

| Runtime address | ROM address | Section |
| --- | --- | --- |
| `0x0800636C` | `0x006CE57C` | `.file_31` |

Native code size: **636 bytes**.

## Interface

A complete callable prototype has not been established. Use the observation or callback-identity pattern in the example rather than guessing the native arguments.

## How it works

Polls destruction flag 0x16B and eventually schedules the final wait followed by func_080066E0.

This controller is distinct from the health-bearing combat actor. Track its own instance for terminal completion observation.

## Usage example

```c
#include "modding.h"

RECOMP_HOOK("func_0800636C_6CE57C")
void observe_0800636c(void *actor) {
    if (!actor) return;
    /* This is the post-fight controller, not the combat actor.
       Track it only for the current defeat sequence. */
}
```

Complete C syntax example using the documented native declarations. Integrate it only at the described engine lifecycle boundary; the game supplies symbol definitions and resident resources.

## Notes

- The terminal controller can have a different address from the health-bearing boss.
- Example shows the recovered call pattern hook boundary, not a full recovered native function prototype.

## Related symbols

- [`func_0800432C_6CC53C`](../functions/func_0800432C_6CC53C.md)
- [`func_080066E0_6CE8F0`](../functions/func_080066E0_6CE8F0.md)
