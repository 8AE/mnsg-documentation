{
  "func_800240DC_24CDC": {
    "title": "Read a game flag",
    "summary": "Tests one bit in the game's packed flag bank at D_8015C608_15D208. Returns zero when the flag is clear, or the bit mask when it is set. Use this to query progression, encounter, and event state without changing it.",
    "confidence": "decompiled",
    "behavior": [
      "For a nonnegative flag ID, the function selects byte (flag_id / 8) & 0xFF, then ANDs it with 1 << (flag_id & 7). Consequently a set bit can return 1, 2, 4, 8, 16, 32, 64, or 128. The native return is not a normalized Boolean.",
      "The bank covers 256 bytes, or 2,048 bit positions. The native implementation masks the byte index rather than validating the input. IDs at or above 0x800 alias earlier bytes; negative arguments follow the compiler's signed division and remainder path and are not meaningful public inputs.",
      "Use the reader to query encounter-active, defeated, reward, or other documented progression flags. Reading a bit does not execute the event associated with it."
    ],
    "parameters": [
      {
        "name": "flag_id",
        "type": "int",
        "description": "Bit index into the game flag bank. Pass a known game flag in the range 0x000–0x7FF, not a byte offset or bit mask."
      }
    ],
    "returns": "Zero if clear; the nonzero bit mask if set. Compare the result with zero.",
    "cautions": [
      "Do not compare a set flag with == 1; flags whose bit position is not zero return larger masks.",
      "The native function does not bounds-check IDs. Confirm what a flag controls before relying on it."
    ],
    "example": "extern int func_800240DC_24CDC(int flag_id);\n\n/* Query a known flag ID supplied by your encounter logic. */\nstatic int game_flag_is_set(unsigned int flag_id)\n{\n    if (flag_id >= 0x800u) {\n        return 0;\n    }\n    return func_800240DC_24CDC((int)flag_id) != 0;\n}",
    "exampleExplanation": "This wrapper validates the bank range and normalizes the native mask to 0 or 1. The game owns the flag bank; the wrapper only reads it.",
    "decompilation": "/* Equivalent logic for valid, nonnegative IDs; not a replacement patch. */\nunsigned int byte_index = ((unsigned int)flag_id >> 3) & 0xFFu;\nunsigned int mask = 1u << ((unsigned int)flag_id & 7u);\nreturn D_8015C608_15D208[byte_index] & mask;",
    "decompilationNote": "Live Ghidra decompilation of resident address 0x800240DC, captured 2026-09-04. Displayed logic is simplified for nonnegative IDs; the native function also contains signed division/remainder adjustments.",
    "related": [
      "func_80024038_24C38",
      "func_80024088_24C88",
      "D_8015C608_15D208",
      "D_800C7AB2"
    ]
  },
  "func_80024038_24C38": {
    "title": "Set a game flag",
    "summary": "Sets one bit in the packed game-flag bank without changing the other bits in that byte. Native event logic uses this entry point to record progression or encounter state.",
    "confidence": "decompiled",
    "behavior": [
      "For valid nonnegative IDs, selects byte (flag_id >> 3) & 0xFF and ORs it with 1 << (flag_id & 7). Setting an already-set flag leaves the bank unchanged.",
      "An entry hook on this function can observe native flag writes. Direct writes to the flag bank bypass that hook; reaching the same stored bit does not necessarily execute the same callbacks.",
      "This routine only changes a bit. Its decompiled body does not save to storage, spawn a reward, or finish a boss death animation."
    ],
    "parameters": [
      {
        "name": "flag_id",
        "type": "int",
        "description": "Known flag bit index, 0x000–0x7FF."
      }
    ],
    "returns": "No return value.",
    "cautions": [
      "There is no native bounds check; larger IDs alias the 256-byte flag bank.",
      "Progression flags can affect later game logic. Set only flags whose meaning and required event sequence you have established."
    ],
    "example": "extern void func_80024038_24C38(int flag_id);\n\nstatic void set_known_game_flag(unsigned int flag_id)\n{\n    if (flag_id < 0x800u) {\n        func_80024038_24C38((int)flag_id);\n    }\n}",
    "exampleExplanation": "Call from game logic with a documented flag ID when the associated event is actually complete. This helper validates the range, but does not perform the event or persist a save.",
    "decompilation": "/* Equivalent logic for valid, nonnegative IDs. */\nunsigned int byte_index = ((unsigned int)flag_id >> 3) & 0xFFu;\nD_8015C608_15D208[byte_index] |=\n    (unsigned char)(1u << ((unsigned int)flag_id & 7u));",
    "decompilationNote": "Live Ghidra decompilation of resident address 0x80024038, captured 2026-09-04; simplified for valid nonnegative IDs.",
    "related": [
      "func_800240DC_24CDC",
      "func_80024088_24C88",
      "D_8015C608_15D208"
    ]
  },
  "func_80024088_24C88": {
    "title": "Clear a game flag",
    "summary": "Clears one bit in the packed game-flag bank, preserving other flags. Use it when a documented game-state transition requires a flag to become clear.",
    "confidence": "decompiled",
    "behavior": [
      "For a valid nonnegative ID, reads byte (flag_id >> 3) & 0xFF and ANDs it with the inverse of 1 << (flag_id & 7). Clearing a clear flag has no additional effect.",
      "Clearing a completion bit alone does not restore an actor or reconstruct an encounter. Actor state, room state, and event state have separate lifecycles."
    ],
    "parameters": [
      {
        "name": "flag_id",
        "type": "int",
        "description": "Known flag bit index, 0x000–0x7FF."
      }
    ],
    "returns": "No return value.",
    "cautions": [
      "IDs are not bounds-checked by the native routine.",
      "Do not treat this as a generic boss reset: actor state, room state, and other progression bits can remain changed."
    ],
    "example": "extern void func_80024088_24C88(int flag_id);\n\nstatic void clear_known_game_flag(unsigned int flag_id)\n{\n    if (flag_id < 0x800u) {\n        func_80024088_24C88((int)flag_id);\n    }\n}",
    "exampleExplanation": "Use with a documented transient flag in a game-state transition you control. The range check does not establish that clearing a particular flag is appropriate.",
    "decompilation": "/* Equivalent logic for valid, nonnegative IDs. */\nunsigned int byte_index = ((unsigned int)flag_id >> 3) & 0xFFu;\nD_8015C608_15D208[byte_index] &=\n    (unsigned char)~(1u << ((unsigned int)flag_id & 7u));",
    "decompilationNote": "Live Ghidra decompilation of resident address 0x80024088, captured 2026-09-04; simplified for valid nonnegative IDs.",
    "related": [
      "func_80024038_24C38",
      "func_800240DC_24CDC",
      "D_8015C608_15D208"
    ]
  },
  "D_800C7AB2": {
    "title": "Current game room ID",
    "summary": "The game’s 16-bit current room identifier. Read it to limit behavior to a particular room or invalidate cached actor state after a transition.",
    "confidence": "source-reviewed",
    "behavior": [
      "Compare this value with a known room constant before applying room-specific behavior. Store it alongside cached actors or pending effects and revalidate that state when the value changes.",
      "Room identity does not establish that actor initialization has completed. Loaded resources, task generation, and encounter phase need their own checks.",
      "The storage is accessed as a 16-bit short. Use unsigned short for room identifiers and keep declarations consistent within your project. The complete surrounding system structure is not required for a read-only room check."
    ],
    "cautions": [
      "Reading a new room ID does not prove that its actor data has finished loading. Check native lifecycle and resource readiness separately.",
      "Writing the variable directly is not a supported room-transition recipe. The engine manages additional transition state."
    ],
    "example": "#include \"modding.h\"\n\nextern unsigned short D_800C7AB2;\n\nstatic unsigned short last_room;\nstatic int have_room;\nstatic unsigned int room_generation;\n\nRECOMP_HOOK_RETURN(\"func_80002040_2C40\")\nvoid track_game_room(void)\n{\n    unsigned short room = D_800C7AB2;\n    if (!have_room || room != last_room) {\n        last_room = room;\n        have_room = 1;\n        ++room_generation;\n        /* Invalidate your room-scoped caches here. */\n    }\n}",
    "exampleExplanation": "Tracks room transitions after the system update and increments a local generation counter. Discard or revalidate cached native actors when that generation changes.",
    "related": [
      "func_80002040_2C40",
      "D_8015C608_15D208"
    ]
  }
}
